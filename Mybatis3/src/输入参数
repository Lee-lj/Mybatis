parameterType，
              parameterType为输入参数的类型  resultType查询后返回结果的值的类型 
                                          如果输入是简单类型（8种基本类型+String）可以使用任意的占位符 #{xxx}，输入简单类型使用#{xxxx}，或者${value} 
                                          如果输入是对象类型，则必须是对象的属性 #{属性名}例如增加操作输入的是对象类型